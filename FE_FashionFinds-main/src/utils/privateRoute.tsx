// import { Route, Navigate } from 'react-router-dom';

// const PrivateRoute = () => (
//     <Route {...rest} element={isAuthenticated ? <Component /> : <Navigate to="/signin" />} />
// );

// export default PrivateRoute;